import React from 'react'

const themeStyles = {
    position: 'absolute',
    bottom: 0,
    backgroundColor: '#111',
    color: '#ddd',
}

const Footer = () => {
  return (
    <footer style={themeStyles}>Footer</footer>
  )
}

export default Footer